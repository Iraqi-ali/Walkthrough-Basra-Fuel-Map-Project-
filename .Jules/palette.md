## 2024-09-03 - Missing ARIA labels
**Learning:** Icon-only buttons and the search input lack `aria-label` attributes, which negatively impacts screen reader accessibility.
**Action:** Always add explicit `aria-label` attributes to inputs without explicit `<label>` tags and icon-only buttons to ensure they are accessible.
